/*
Theme Name: Solux Energy
Theme URI: https://soluxenergy.com.br
Author: Solux Energy
Author URI: https://soluxenergy.com.br
Description: Tema premium para empresa de energia solar - Dark Mode com estética de luxo
Version: 1.0.0
License: GNU General Public License v2 or later
License URI: http://www.gnu.org/licenses/gpl-2.0.html
Text Domain: solux-energy
Tags: dark-mode, solar, energy, premium, luxury
*/

/* ==========================================================================
   CSS Variables / Design Tokens
   ========================================================================== */
:root {
  /* Primary Colors */
  --forest: 160 84% 10%;
  --forest-light: 158 64% 15%;
  --gold: 38 92% 50%;
  --gold-light: 45 93% 58%;
  --gold-dark: 32 81% 42%;
  
  /* Semantic Colors */
  --background: 160 84% 8%;
  --foreground: 210 40% 98%;
  --primary: 38 92% 50%;
  --primary-foreground: 160 84% 10%;
  --secondary: 160 30% 20%;
  --secondary-foreground: 210 40% 98%;
  --muted: 160 30% 15%;
  --muted-foreground: 215 20% 65%;
  --accent: 38 92% 50%;
  --accent-foreground: 160 84% 10%;
  --danger: 0 84% 60%;
  --success: 142 76% 36%;
  --light: 210 40% 98%;
  --light-muted: 210 40% 96%;
  
  /* Spacing */
  --section-padding: 5rem 0;
  --container-max: 1200px;
  
  /* Border Radius */
  --radius: 0.5rem;
  --radius-lg: 1rem;
  
  /* Transitions */
  --transition: all 0.3s ease;
}

/* ==========================================================================
   Reset & Base
   ========================================================================== */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  font-size: 16px;
  line-height: 1.6;
  color: hsl(var(--foreground));
  background-color: hsl(var(--background));
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

h1, h2, h3, h4, h5, h6 {
  font-family: 'Montserrat', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  font-weight: 700;
  line-height: 1.2;
}

a {
  color: inherit;
  text-decoration: none;
  transition: var(--transition);
}

img {
  max-width: 100%;
  height: auto;
  display: block;
}

/* ==========================================================================
   Utility Classes
   ========================================================================== */
.container {
  width: 100%;
  max-width: var(--container-max);
  margin: 0 auto;
  padding: 0 1.5rem;
}

.text-gold {
  color: hsl(var(--gold));
}

.text-danger {
  color: hsl(var(--danger));
}

.bg-forest {
  background-color: hsl(var(--forest));
}

.bg-light {
  background-color: hsl(var(--light));
}

.section {
  padding: var(--section-padding);
}

/* ==========================================================================
   Header
   ========================================================================== */
.site-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  background: hsl(var(--forest) / 0.8);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid hsl(var(--gold) / 0.1);
  transition: var(--transition);
}

.site-header.scrolled {
  background: hsl(var(--forest) / 0.95);
}

.header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 80px;
}

.site-logo {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.site-logo svg {
  width: 32px;
  height: 32px;
  color: hsl(var(--gold));
}

.site-logo span {
  font-family: 'Montserrat', sans-serif;
  font-size: 1.5rem;
  font-weight: 700;
  color: hsl(var(--light));
}

.site-logo .highlight {
  color: hsl(var(--gold));
}

.main-nav {
  display: none;
}

@media (min-width: 768px) {
  .main-nav {
    display: flex;
    align-items: center;
    gap: 2rem;
  }
}

.main-nav a {
  font-size: 0.875rem;
  font-weight: 500;
  color: hsl(var(--light) / 0.8);
  transition: var(--transition);
}

.main-nav a:hover {
  color: hsl(var(--gold));
}

.header-cta {
  display: none;
}

@media (min-width: 768px) {
  .header-cta {
    display: block;
  }
}

.mobile-menu-toggle {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  background: transparent;
  border: none;
  color: hsl(var(--light));
  cursor: pointer;
}

@media (min-width: 768px) {
  .mobile-menu-toggle {
    display: none;
  }
}

/* ==========================================================================
   Buttons
   ========================================================================== */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  font-family: 'Montserrat', sans-serif;
  font-size: 0.875rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  border: none;
  border-radius: var(--radius);
  cursor: pointer;
  transition: var(--transition);
}

.btn-primary {
  background: linear-gradient(135deg, hsl(var(--gold)), hsl(var(--gold-dark)));
  color: hsl(var(--forest));
  box-shadow: 0 4px 20px hsl(var(--gold) / 0.3);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 30px hsl(var(--gold) / 0.4);
}

.btn-secondary {
  background: transparent;
  color: hsl(var(--gold));
  border: 2px solid hsl(var(--gold) / 0.5);
}

.btn-secondary:hover {
  background: hsl(var(--gold) / 0.1);
  border-color: hsl(var(--gold));
}

.btn-lg {
  padding: 1rem 2rem;
  font-size: 1rem;
}

.btn-xl {
  padding: 1.25rem 3rem;
  font-size: 1.125rem;
}

/* ==========================================================================
   Hero Section
   ========================================================================== */
.hero-section {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding-top: 80px;
  overflow: hidden;
}

.hero-bg {
  position: absolute;
  inset: 0;
  z-index: -1;
}

.hero-bg img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    135deg,
    hsl(var(--forest) / 0.95) 0%,
    hsl(var(--forest) / 0.85) 50%,
    hsl(var(--forest) / 0.7) 100%
  );
}

.hero-content {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: 1fr;
  gap: 3rem;
  align-items: center;
}

@media (min-width: 1024px) {
  .hero-content {
    grid-template-columns: 1fr 1fr;
  }
}

.hero-text h1 {
  font-size: clamp(2rem, 5vw, 3.5rem);
  margin-bottom: 1.5rem;
  color: hsl(var(--light));
}

.hero-text h1 .highlight {
  display: block;
  background: linear-gradient(135deg, hsl(var(--gold)), hsl(var(--gold-light)));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.hero-text p {
  font-size: 1.125rem;
  color: hsl(var(--light) / 0.8);
  margin-bottom: 2rem;
  max-width: 500px;
}

.hero-form-card {
  background: hsl(var(--forest-light) / 0.8);
  backdrop-filter: blur(12px);
  border: 1px solid hsl(var(--gold) / 0.2);
  border-radius: var(--radius-lg);
  padding: 2rem;
}

.hero-form-card h3 {
  font-size: 1.25rem;
  color: hsl(var(--gold));
  margin-bottom: 0.5rem;
}

.hero-form-card p {
  color: hsl(var(--light) / 0.7);
  font-size: 0.875rem;
  margin-bottom: 1.5rem;
}

/* ==========================================================================
   Forms
   ========================================================================== */
.form-group {
  margin-bottom: 1rem;
}

.form-label {
  display: block;
  font-size: 0.875rem;
  font-weight: 500;
  color: hsl(var(--light) / 0.8);
  margin-bottom: 0.5rem;
}

.form-input,
.form-select {
  width: 100%;
  padding: 0.75rem 1rem;
  font-size: 1rem;
  color: hsl(var(--light));
  background: hsl(var(--forest));
  border: 1px solid hsl(var(--gold) / 0.3);
  border-radius: var(--radius);
  transition: var(--transition);
}

.form-input::placeholder {
  color: hsl(var(--light) / 0.4);
}

.form-input:focus,
.form-select:focus {
  outline: none;
  border-color: hsl(var(--gold));
  box-shadow: 0 0 0 3px hsl(var(--gold) / 0.1);
}

/* ==========================================================================
   Pain Section
   ========================================================================== */
.pain-section {
  background: hsl(var(--light));
  color: hsl(160 84% 10%);
}

.section-header {
  text-align: center;
  max-width: 700px;
  margin: 0 auto 3rem;
}

.section-header h2 {
  font-size: clamp(1.75rem, 4vw, 2.5rem);
  margin-bottom: 1rem;
}

.section-header p {
  font-size: 1.125rem;
  opacity: 0.8;
}

.comparison-chart {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  max-width: 600px;
  margin: 0 auto 2rem;
}

.chart-bar {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.chart-label {
  display: flex;
  justify-content: space-between;
  font-weight: 600;
}

.chart-bar-fill {
  height: 3rem;
  border-radius: var(--radius);
  display: flex;
  align-items: center;
  padding: 0 1rem;
  font-weight: 700;
  color: white;
}

.chart-bar-fill.danger {
  background: linear-gradient(90deg, hsl(var(--danger)), hsl(0 70% 50%));
  width: 100%;
}

.chart-bar-fill.success {
  background: linear-gradient(90deg, hsl(var(--success)), hsl(142 60% 45%));
  width: 35%;
}

/* ==========================================================================
   Opportunity Cost Section
   ========================================================================== */
.opportunity-section {
  background: hsl(var(--forest));
}

.opportunity-section .section-header h2,
.opportunity-section .section-header p {
  color: hsl(var(--light));
}

.opportunity-cards {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1.5rem;
}

@media (min-width: 768px) {
  .opportunity-cards {
    grid-template-columns: repeat(3, 1fr);
  }
}

.opportunity-card {
  background: hsl(var(--light));
  border-radius: var(--radius-lg);
  padding: 2rem;
  text-align: center;
  transition: var(--transition);
}

.opportunity-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 40px hsl(0 0% 0% / 0.3);
}

.opportunity-card-icon {
  width: 64px;
  height: 64px;
  margin: 0 auto 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, hsl(var(--gold) / 0.2), hsl(var(--gold) / 0.1));
  border-radius: 50%;
  color: hsl(var(--gold-dark));
}

.opportunity-card-icon svg {
  width: 32px;
  height: 32px;
}

.opportunity-card-badge {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  background: hsl(var(--gold) / 0.1);
  color: hsl(var(--gold-dark));
  font-size: 0.75rem;
  font-weight: 600;
  border-radius: 9999px;
  margin-bottom: 1rem;
}

.opportunity-card h3 {
  font-size: 1.25rem;
  color: hsl(var(--forest));
  margin-bottom: 0.5rem;
}

.opportunity-card p {
  font-size: 0.875rem;
  color: hsl(var(--forest) / 0.7);
}

/* ==========================================================================
   Testimonials Section
   ========================================================================== */
.testimonials-section {
  background: linear-gradient(180deg, hsl(var(--forest)), hsl(var(--forest-light)));
}

.testimonials-section .section-header h2,
.testimonials-section .section-header p {
  color: hsl(var(--light));
}

.testimonials-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1.5rem;
}

@media (min-width: 768px) {
  .testimonials-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.testimonial-card {
  position: relative;
  aspect-ratio: 9 / 16;
  border-radius: var(--radius-lg);
  overflow: hidden;
  cursor: pointer;
}

.testimonial-card img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.testimonial-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to top,
    hsl(0 0% 0% / 0.8) 0%,
    hsl(0 0% 0% / 0.3) 50%,
    hsl(0 0% 0% / 0.1) 100%
  );
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 1.5rem;
}

.testimonial-play {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 64px;
  height: 64px;
  background: hsl(var(--gold) / 0.9);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: hsl(var(--forest));
  transition: var(--transition);
}

.testimonial-card:hover .testimonial-play {
  transform: translate(-50%, -50%) scale(1.1);
  background: hsl(var(--gold));
}

.testimonial-info h4 {
  color: hsl(var(--light));
  font-size: 1rem;
  margin-bottom: 0.25rem;
}

.testimonial-info p {
  color: hsl(var(--gold));
  font-size: 0.875rem;
  font-weight: 600;
}

/* ==========================================================================
   Technology Section
   ========================================================================== */
.technology-section {
  background: hsl(var(--light));
  color: hsl(var(--forest));
}

.technology-content {
  display: grid;
  grid-template-columns: 1fr;
  gap: 3rem;
  align-items: center;
}

@media (min-width: 1024px) {
  .technology-content {
    grid-template-columns: 1fr 1fr;
  }
}

.technology-features {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.feature-item {
  display: flex;
  gap: 1rem;
  align-items: flex-start;
}

.feature-icon {
  flex-shrink: 0;
  width: 48px;
  height: 48px;
  background: linear-gradient(135deg, hsl(var(--gold)), hsl(var(--gold-dark)));
  border-radius: var(--radius);
  display: flex;
  align-items: center;
  justify-content: center;
  color: hsl(var(--forest));
}

.feature-item h4 {
  font-size: 1rem;
  margin-bottom: 0.25rem;
}

.feature-item p {
  font-size: 0.875rem;
  opacity: 0.7;
}

.technology-image {
  position: relative;
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.technology-image img {
  width: 100%;
  height: auto;
}

.technology-badge {
  position: absolute;
  bottom: 1.5rem;
  left: 1.5rem;
  right: 1.5rem;
  background: hsl(var(--forest) / 0.95);
  backdrop-filter: blur(8px);
  border: 1px solid hsl(var(--gold) / 0.3);
  border-radius: var(--radius);
  padding: 1rem;
  display: flex;
  align-items: center;
  gap: 1rem;
}

.technology-badge-icon {
  width: 40px;
  height: 40px;
  background: hsl(var(--gold));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: hsl(var(--forest));
}

.technology-badge-text h5 {
  color: hsl(var(--gold));
  font-size: 0.875rem;
}

.technology-badge-text p {
  color: hsl(var(--light) / 0.7);
  font-size: 0.75rem;
}

/* ==========================================================================
   Concierge Section
   ========================================================================== */
.concierge-section {
  background: hsl(var(--forest));
}

.concierge-section .section-header h2,
.concierge-section .section-header p {
  color: hsl(var(--light));
}

.process-timeline {
  display: grid;
  grid-template-columns: 1fr;
  gap: 2rem;
}

@media (min-width: 768px) {
  .process-timeline {
    grid-template-columns: repeat(4, 1fr);
  }
}

.process-step {
  text-align: center;
  position: relative;
}

@media (min-width: 768px) {
  .process-step:not(:last-child)::after {
    content: '';
    position: absolute;
    top: 2rem;
    right: -1rem;
    width: 2rem;
    height: 2px;
    background: linear-gradient(90deg, hsl(var(--gold)), transparent);
  }
}

.process-step-number {
  width: 64px;
  height: 64px;
  margin: 0 auto 1rem;
  background: linear-gradient(135deg, hsl(var(--gold)), hsl(var(--gold-dark)));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'Montserrat', sans-serif;
  font-size: 1.5rem;
  font-weight: 700;
  color: hsl(var(--forest));
}

.process-step h4 {
  color: hsl(var(--light));
  font-size: 1rem;
  margin-bottom: 0.5rem;
}

.process-step p {
  color: hsl(var(--light) / 0.7);
  font-size: 0.875rem;
}

/* ==========================================================================
   Contact Section
   ========================================================================== */
.contact-section {
  background: linear-gradient(135deg, hsl(var(--forest-light)), hsl(var(--forest)));
  position: relative;
  overflow: hidden;
}

.contact-section::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -25%;
  width: 50%;
  height: 200%;
  background: radial-gradient(circle, hsl(var(--gold) / 0.1) 0%, transparent 70%);
  pointer-events: none;
}

.contact-content {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: 1fr;
  gap: 3rem;
  align-items: center;
}

@media (min-width: 1024px) {
  .contact-content {
    grid-template-columns: 1fr 1fr;
  }
}

.contact-text h2 {
  font-size: clamp(2rem, 4vw, 3rem);
  color: hsl(var(--light));
  margin-bottom: 1rem;
}

.contact-text h2 .highlight {
  color: hsl(var(--gold));
}

.contact-text p {
  color: hsl(var(--light) / 0.8);
  margin-bottom: 2rem;
}

.contact-benefits {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.contact-benefit {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  color: hsl(var(--light) / 0.9);
}

.contact-benefit svg {
  color: hsl(var(--gold));
  flex-shrink: 0;
}

.contact-form-card {
  background: hsl(var(--forest) / 0.5);
  backdrop-filter: blur(12px);
  border: 1px solid hsl(var(--gold) / 0.2);
  border-radius: var(--radius-lg);
  padding: 2rem;
}

/* ==========================================================================
   Footer
   ========================================================================== */
.site-footer {
  background: hsl(160 84% 6%);
  border-top: 1px solid hsl(var(--gold) / 0.1);
  padding: 3rem 0 1.5rem;
}

.footer-content {
  display: grid;
  grid-template-columns: 1fr;
  gap: 2rem;
  margin-bottom: 2rem;
}

@media (min-width: 768px) {
  .footer-content {
    grid-template-columns: 2fr 1fr 1fr;
  }
}

.footer-brand p {
  color: hsl(var(--light) / 0.6);
  font-size: 0.875rem;
  margin-top: 1rem;
  max-width: 300px;
}

.footer-links h4 {
  color: hsl(var(--gold));
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 1rem;
}

.footer-links ul {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.footer-links a {
  color: hsl(var(--light) / 0.6);
  font-size: 0.875rem;
}

.footer-links a:hover {
  color: hsl(var(--gold));
}

.footer-bottom {
  padding-top: 1.5rem;
  border-top: 1px solid hsl(var(--light) / 0.1);
  display: flex;
  flex-direction: column;
  gap: 1rem;
  align-items: center;
  text-align: center;
}

@media (min-width: 768px) {
  .footer-bottom {
    flex-direction: row;
    justify-content: space-between;
    text-align: left;
  }
}

.footer-bottom p {
  color: hsl(var(--light) / 0.4);
  font-size: 0.75rem;
}

.footer-social {
  display: flex;
  gap: 1rem;
}

.footer-social a {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: hsl(var(--light) / 0.1);
  border-radius: 50%;
  color: hsl(var(--light) / 0.6);
  transition: var(--transition);
}

.footer-social a:hover {
  background: hsl(var(--gold));
  color: hsl(var(--forest));
}

/* ==========================================================================
   Animations
   ========================================================================== */
@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes pulseGlow {
  0%, 100% {
    box-shadow: 0 0 20px hsl(var(--gold) / 0.3);
  }
  50% {
    box-shadow: 0 0 40px hsl(var(--gold) / 0.5);
  }
}

.animate-fade-up {
  animation: fadeUp 0.6s ease-out forwards;
}

.animate-pulse-glow {
  animation: pulseGlow 2s ease-in-out infinite;
}

/* Scroll reveal classes */
.reveal {
  opacity: 0;
  transform: translateY(30px);
  transition: all 0.6s ease-out;
}

.reveal.active {
  opacity: 1;
  transform: translateY(0);
}

/* ==========================================================================
   Responsive Utilities
   ========================================================================== */
@media (max-width: 767px) {
  .hide-mobile {
    display: none !important;
  }
}

@media (min-width: 768px) {
  .hide-desktop {
    display: none !important;
  }
}
